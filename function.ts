function addTwo(num:number){
    // num.toUpperCase();
    return num + 2;
}

addTwo(5);
// function signinUser(name:string, salery: number, isPaid: boolean = false){}
// let loginUser = (name:string, salery: number, isPaid: boolean = false) => {}


function signinUser(name:string, salery: number, isPaid: boolean = false):string{
    return "hi"
}

let loginUser = (name:string, salery: number, isPaid: boolean = false):number => {
    return 5
}
signinUser("dhruv", 3000)

export{}